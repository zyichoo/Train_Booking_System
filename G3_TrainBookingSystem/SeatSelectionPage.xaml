<Page
    x:Class="G3_TrainBookingSystem.SeatSelectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:G3_TrainBookingSystem"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <!-- Style for the PivotHeaderItem -->
        <Style TargetType="PivotHeaderItem">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="4,4,4,6"/>
        </Style>
    </Page.Resources>

    <!-- Main Layout Grid -->
    <Grid x:Name="HeaderGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Grid.Row="0">
            <TextBlock x:Name="SelectedTrainDetails" FontSize="20" FontWeight="Bold" TextAlignment="Center"/>
            <TextBlock x:Name="txtDepartureTime" FontSize="18" FontStyle="Italic" Foreground="Gray" TextAlignment="Center"/>
            <TextBlock x:Name="txtArrivalTime" FontSize="18" FontStyle="Italic" Foreground="Gray" TextAlignment="Center"/>
            <TextBlock x:Name="txtDuration" FontSize="18" FontStyle="Italic" Foreground="Gray" TextAlignment="Center"/>
            <TextBlock x:Name="txtTicketPrice" FontSize="18" FontStyle="Italic" Foreground="Gray" TextAlignment="Center"/>
        </StackPanel>

        <TextBlock Grid.Row="1" HorizontalAlignment="Center" Text="Choose Your Preferred Seat" FontSize="20" FontWeight="Bold" Margin="10" />

        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto">
            <Pivot x:Name="CoachPivot" HorizontalAlignment="Center" MaxWidth="450">
                <!-- Coaches will be dynamically added here in the code-behind -->
            </Pivot>
        </ScrollViewer>

        <Button Grid.Row="3" Content="Back" HorizontalAlignment="Left" Click="OnBackClicked" Margin="10,0,0,10"/>
    </Grid>
</Page>
